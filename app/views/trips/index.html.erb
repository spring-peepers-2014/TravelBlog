<h1>Trips Index Page</h1>
<%= link_to "New Trip", new_trip_path, id: "new-trip"%>

<ul class='list-group'>
  <% @trips.each do |trip| %>
    <li class="list-group-item"><%= link_to trip.title, trip_path(trip)%></li>
  <% end %>
</ul>
