  <nav class="navbar navbar-inverse navbar-fixed-top" role="navigation">
    <div class="container-fluid">
      <div class="navbar-header">
        <ul class="nav navbar-nav">
          <li><%= link_to image_tag("geoscribe.png"), root_path, id: "nav-logo" %></li>
        </ul>
      </div>
        <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-5">
          <% if current_user %>
            <p class="navbar-text navbar-right">Welcome <%= current_user.email %> <%= link_to "Logout", '/users/logout', html: {class: 'navbar-link'} %></p>
          <% else %>
            <p class="navbar-text navbar-right"><%= link_to "Login/Signup", '/users/new', html: {class: 'navbar-link'} %></p>
          <% end %>
        </div>
    </div>
  </nav>